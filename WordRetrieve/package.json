{
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "mkdirp": "^0.5.1",
    "xmldom": "^0.1.27"
  },
  "scripts": {
    "dev": "babel src -d dist && node dist/spellingcity.js",
    "dev1": "babel src -d dist && node dist/worldListMerge.js"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.1"
  }
}
